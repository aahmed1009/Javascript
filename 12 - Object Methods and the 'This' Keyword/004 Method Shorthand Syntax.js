//object math masln shorthand
//FIXME:SHORTHAND
const math = {
  add(x, y) {
    return x + y;
  },
  subtract(x, y) {
    return x - y;
  },
  multiply(x, y) {
    return x * y;
  },
};
console.log(math.add(5, 5));
console.log(math.subtract(5, 5));
console.log(math.multiply(5, 5));
